{
  "name": "@liquify/schema-stores",
  "version": "0.0.1",
  "description": "JSON Schema Store files for the Liquify IDE package/extension",
  "main": "./package/index.js",
  "scripts": {
    "_watch": "liquify -i stores -o package -w",
    "_buildss": "liquify -i stores -o package -w",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Nikolas Savvidis <nicos@gmx.com>",
  "private": true,
  "repository": {
    "url": "https://github.com/panoply/liquify"
  },
  "devDependencies": {
    "rollup-plugin-terser": "^6.0.1"
  }
}
