{
  "name": "@liquify/liquid-language-grammars",
  "version": "0.0.0",
  "description": "TextMate Language grammars for the Liquid Templating Language",
  "author": "Nikolas Savvidis <nicos@gmx.com>",
  "license": "MIT",
  "private": true,
  "files": [
    "package"
  ],
  "repository": {
    "url": "https://github.com/panoply/liquid-language-grammars"
  },
  "scripts": {
    "dev": "liquify dev -c build.grammar.json -i syntax -o package -w",
    "build": "liquify build -c build.grammar.json -i syntax -o package --prod",
    "peep": "liquify peek",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "husky": {
    "hooks": {
      "post-merge": "pnpm run push"
    }
  },
  "devDependencies": {
    "@liquify/cli": "workspace:^0.0.1"
  },
  "peerDependencies": {
    "@liquify/cli": "workspace:^0.0.1"
  }
}
