{
  "name": "@liquify/liquid-language-grammars",
  "version": "0.0.1",
  "description": "TextMate Language grammars for the Liquid Templating Language",
  "main": "./package/index.js",
  "scripts": {
    "build": "liquify grammar -m=generate.jsonc -o=package",
    "create": "liquify grammar create -o=package -b",
    "watch": "liquify grammar -m=generate.jsonc -o=package -w",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Nikolas Savvidis <nicos@gmx.com>",
  "license": "MIT",
  "dependencies": {
    "@liquify/cli": "link:../../cli"
  }
}
