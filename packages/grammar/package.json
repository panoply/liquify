{
  "name": "@liquify/language-grammars",
  "version": "0.0.1",
  "description": "TextMate Language grammars for the Liquid Templating Language",
  "main": "./package/index.js",
  "scripts": {
    "watch": "liquify grammar --input=src/ --output=package/ --watch",
    "build": "liquify grammar --input=src/ --output=package/ --build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Nikolas Savvidis <nicos@gmx.com>",
  "license": "MIT",
  "devDependencies": {
    "@liquify/cli": "^0.0.1"
  }
}
