{%- assign item = section.settings    -%}
{%- assign size = item.height | prepend: 'gallery-{{ tag }}' -%}
"{{ test }}"
<script>

</script>

<div
  class="row justify-content-center no-gutters">
  {%- for block in section.blocks     -%}
    <div
      class="col">
      <div
        class="content">

        {% render 'file1'
          , my_variable: product.available
          , my_other_variable: 'oranges'
        %}

        <div
          class="{{- item.type }} d-flex {{ align }} justify-content-center"
          style="background-color: rgba(0, 0, 0, {{ item.overlay }});">
          <a
            href="{{- collection.url -}}">
            {%- if block.settings.title -%}
              {{ block.settings.title }}
            {%- else -%}
              {{ block.settings.collection }}
            {%- endif -%}
          </a>
        </div>
        <div
          class="aspect-ratio">
          {%- if block.settings.image -%}
            <img
              alt="{{- block.settings.image.alt -}}"
              class="img-fluid image"
              src="{{- block.settings.image | img_url: '' -}}">
          {%- else -%}
            {%- if collection.image -%}
              <img
                alt="{{- collection.image.alt -}}"
                class="img-fluid image"
                src="{{- collection.image.src | collection_img_url: 'original' -}}">
            {%- else -%}
              <a
                href="{{- collection.url -}}">
                {{ block.settings.collection }}
              </a>
            {%- endif -%}
          {%- endif    -%}
        </div>
      </div>
    </div>
  {%- endfor -%}
</div>

<script data-attr="test">
const test =
</script>

