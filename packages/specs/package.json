{
  "name": "@liquify/liquid-language-specs",
  "version": "0.0.0",
  "private": false,
  "author": "Nikolas Savvidis <nicos@gmx.com>",
  "license": "SEE LICENSE IN LICENSE",
  "description": "Liquid language variation specification references used by Liquify",
  "main": "package/index.js",
  "engines": {
    "pnpm": ">=4",
    "node": "12"
  },
  "repository": {
    "url": "https://github.com/panoply/liquify"
  },
  "scripts": {
    "dev": "rollup -c -w",
    "devs": "liquify dev -i variations -o package --watch",
    "build": "liquify build -i variations -o package --prod",
    "peek": "liquify peek -i package",
    "pack": "cd versions; pnpm pack ../ && cd ..",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "rollup-plugin-terser": "^6.0.1"
  }
}
