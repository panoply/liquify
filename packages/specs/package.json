{
  "name": "@liquify/specs",
  "version": "0.0.2",
  "engines": {
    "node": "12"
  },
  "description": "Liquid language variation specification references for the Liquid Language Server and Liquify IDE extension/package tool",
  "main": "package/index.js",
  "scripts": {
    "peek": "liquify peek -i=package/",
    "watch": "liquify specs -i=variations/ -o=package/ -w",
    "build": "liquify specs -i=variations/ -o=package/ -b",
    "pack": "cd versions; pnpm pack ../ && cd ..",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Nikolas Savvidis <nicos@gmx.com>",
  "license": "SEE LICENSE IN LICENSE",
  "dependencies": {
    "@liquify/cli": "link:../../cli"
  }
}
