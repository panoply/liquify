---
foo: bar
---

{% increment %}

{% comment %}
  @format {

  }
{% endcomment %}

{{- hello | filter: 'something' |  replace: 'foo', 'bar' |  num: 1 -}}

{{ dd | f: 1234 |  singluar  }}

{% include 'foo/bar' param: false, something: 'new' %}
{% include foo/bar %}



{% if test == 12340 and foo == false or test != null %}

{%- for foo in bar reverse -%}

  {{ foo | some: true }}

{%- endfor -%}

{% for variant in product.variants %}
  {% if variant.available %}
    {% assign variant_available = true  %}
  {% else %}
    {% assign variant_available = false %}
  {% endif %}
{% endfor %}

{% if variant_available == false %}
  {% unless product.tags contains 'discontinued' %}
    {% include 'iStock-notify-me' %}
  {% endunless %}
{% endif %}
