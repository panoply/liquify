<!-- MISSING START TAG -->
{%- endif -%}

<!-- MISSING END TAG -->
{%- if -%}

<!-- WITHIN ITERATION MULTIPLE LEVELS DEEP -->
{%- if tag == 'foo' -%}
  {%- for tag in collections.foo.bar -%}
    {%- if tag contains garment -%}
      {%- for tag in collections.foo.bar -%}
        <!-- MISSING END TAG -->
        {%- if tag contains garment -%}
      {%- endfor -%}
    {%- endif -%}
  {%- endfor -%}
{%- endif -%}

{%- if tag == 'foo' -%}
  <!-- MISSING END TAG -->
  {%- for tag in collections.foo.bar -%}
    {%- if tag contains garment -%}
      {%- for tag in collections.foo.bar -%}
        <!-- MISSING END TAG -->
        {%- if tag contains garment -%}
      {%- endfor -%}
    {%- endif -%}

{%- endif -%}
